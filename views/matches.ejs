<%- include('head.ejs') %>
<body>
  <h1>Your matches</h1>
<% matches.forEach(function (user){ %>
  <section class='user'>
    <h3><%= user.first_name + " " + user.last_name%></h3>
    <p><%= user.gender %></p>
  </section>
<% }); %>
</body>

<%- include('footer.ejs') %>
